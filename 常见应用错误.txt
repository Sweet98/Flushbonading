1.makefile 编写 a.c
test: a.o
cc -o test a.o
a.o:  a.c b.h
cc -c  a.c b.h 
2.java中final作用
3.c语言内存分配
   栈：局部变量 函数形参 
   堆：动态内存 malloc 
   全局数据区： 全局变量 静态变量 常量
  内存错误：
   1.内存定义未初始化
   2.数组越界
   3.用完后不释放内存
   4.释放内存后继续使用
4.七层网络模型及其作用
  
5.多任务同步方式，特点，适用场合
 互斥锁
 条件变量
 信号量
6.volatile关键字作用
  声明变量为易变类型变量，每次调用时，需重新读取而不是假设变量值。
  static 
  静态局部变量
  静态全局变量
  静态函数
  const 常量
  
7.单双工、半双工、全双工
所谓单双工通信是指信息始终是一个方向的通信。听广播和看电视就是单双工通信的典型例子。 
所谓半双工通信是指信息流可以在两个方向传输，但同一时刻只限于一个方向的传输。对讲机记得通信就是半双工通信
所谓全双工通信是指同时可以作双向的通信，即通信的一方在发送信息的同时也能接受信息。 
8.总线结构：
iic: 两根线 一根双向信号线 一根时钟线  半双工
spi：四根线 一根输入 一根输出 一根时钟 一根片选 全双工
9.gdb调试
10.软中断
11.中断服务子程序应满足哪几点
    1、中断请求：中断事件一旦发生或者中断条件一旦构成，中断源提交“申请报告”，与请求CPU暂时放下目前的工作而转为中断源作为专项服务
　　2、中断屏蔽：虽然中断源提交了“申请报告”，但是，是否得到CPU的响应，还要取决于“申请报告”是否能够通过2道或者3道“关卡”（中断屏蔽）送达CPU（相应的中断屏蔽位等于1，为关卡放行；反之相应的中断屏蔽位等于0，为关卡禁止通行）；
　　3、中断响应：如果一路放行，则CPU响应中断后，将被打断的工作断点记录下来（把断点地址保护到堆栈），挂起“不再受理其他申请报告牌”（清除全局中断标志位GIE=0），跳转到中断服务子程序
　　4、保护现场：在处理新任务时可能破坏原有的工作现场，所以需要对工作现场和工作环境进行适当保护；
　　5、调查中断源：检查“申请报告”是由哪个中断源提交的，以便作出有针对性的服务；
　　6、中断处理：开始对查明的中断源进行有针对性的中断服务；
　　7、清除标志：在处理完毕相应的任务之后，需要进行撤消登记（清除中断标志），以避免造成重复响应；
　　8、恢复现场：恢复前面曾经被保护起来的工作现场，以便继续执行被中断的工作；
　　9、中断返回：将被打断的工作断点找回来（从堆栈中恢复断点地址），并摘下“不再受理其他申请报告牌”（GIE＝1），继续执行原先被打断的工作。 
12.#define 和 const区别
  define宏定义和const常变量区别：
1.define是宏定义，程序在预处理阶段将用define定义的内容进行了替换。因此程序运行时，常量表中并没有用define定义的常量，系统不为它分配内存。
  const定义的常量，在程序运行时在常量表中，系统为它分配内存。
2.define定义的常量，预处理时只是直接进行了替换。所以编译时不能进行数据类型检验。
  const定义的常量，在编译时进行严格的类型检验，可以避免出错。
3.define定义表达式时要注意“边缘效应”，例如如下定义：
    #define N 2+3 //我们预想的N值是5，我们这样使用N
    int a = N/2;  //我们预想的a的值是2.5，可实际上a的值是3.5
原因在于在预处理阶段，编译器将 a = N/2处理成了 a = 2+3/2；这就是宏定义的字符串替换的“边缘效应”因此要如下定义：
    #define N (2+3)
const定义表达式没有上述问题。
const定义的常量叫做常变量原因有二：
const定义常量像变量一样检查类型
const可以在任何地方定义常量，编译器对它的处理过程与变量相似，只是分配内存的地方不同 
13.比如tcp和udp的区别，我是这么说的：
14     这个问题可以一句话说明，被volatile关键字修饰的变量可以防止被编译器优化。当然这样说肯定没问题，但如果同时说明可能会更好一点：
     1：编译器为什么要有优化的机制？   
     2：具体是怎么优化的？
     3：volatile又是怎么避免被优化的？
15    1：知道的东西恰到好处的往多了说
     2：设置悬念，自我介绍部分，我是说了学校和专业后马上说籍贯，然后说抛出自己为什么千里迢        迢跑到南京其实有个小故事的问他想不想知道？就算他不感兴趣出于礼貌也还是会让你简单        说一下，然后说高考后为了女朋友，然后他就很好奇的问我高中就谈恋爱了？好吧，我只能        告诉他00后小学就开始了，他很无语。。。然后八卦了一会儿（当然我没告诉他刚来南京我        们就分了，这个留着最后问到家庭情况，工作地点的问题时再抛出来，果然，他很惊讶，很        同情的问我是不是这几年过的不好。。有没有再找。。）其实说这么多就是想说，面试的过        程中尽量让对方也参与到这个过程中，能有情绪的变化最好，比如惊讶，替你惋惜。。把握        聊天的氛围和节奏。这两次面试聊到后来他们都很笑了，就想应该有戏。
     3：还有就是专业术语说的不是很顺溜的话建议自己组织语言，解释清楚问题，效果可能还会更        好点，否则对方怀疑我们是在背书（有一次一个面试官就是当面这么说我的）。

17 const
18 virtual不能修饰那些函数
19 指针和引用的区别
20 线程和进程
21 操作系统的调度算法有哪些，优缺点
22 死锁的必要条件
23 数据结构常用的排序算法有哪些，都有什么区别，挑一个熟悉的讲一下
24 服务器能连接的客户端数量是有限的，怎么突破服务器的这个瓶颈？
25. ，socket三种监听方式